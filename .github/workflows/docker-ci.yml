name: docker-ci

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  docker_ci:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: gateway
            context: ./services/gateway
          - name: xtts
            context: ./services/xtts
          - name: xtts-glue
            context: ./glue
          - name: tts-worker
            context: ./services/tts-worker
          - name: whisper-worker
            context: ./services/whisper-worker
    name: ${{ matrix.name }}
    uses: smk762/gha-docker-shared-ci/.github/workflows/docker-ci.yml@main
    with:
      image: ${{ vars.DOCKERHUB_ORG || github.repository_owner }}/tss-stack-${{ matrix.name }}
      context: ${{ matrix.context }}
      dockerfile: Dockerfile
      compose_files: docker-compose.yml
      trivy_fail_severity: CRITICAL,HIGH
    secrets: inherit

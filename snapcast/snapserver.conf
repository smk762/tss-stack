[stream]
# Snapserver will create/open the FIFO at this path. Glue writes to the same path via the shared volume.
source = pipe:///run/snapcast/snapfifo?name=main&codec=flac&sampleformat=48000:16:2&chunk_ms=20

